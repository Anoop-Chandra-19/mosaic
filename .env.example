# Optional: choose which browser Vite opens when `bun run dev` starts.
# Examples: google-chrome-stable, firefox
BROWSER=google-chrome-stable

# Optional: extra args passed to the browser when opening.
# Example: --incognito
# BROWSER_ARGS=--incognito
